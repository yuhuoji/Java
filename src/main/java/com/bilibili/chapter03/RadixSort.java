package com.bilibili.chapter03;

/* 基数排序
 * 不基于比较的排序 必须要有进制才能比较
 * 时间复杂度O（N）*/
public class RadixSort {
    public void radixSort(int[] arr) {
        if (arr == null || arr.length < 2) {
            return;
        }
//        radixSort(arr, 0, arr.length - 1, maxbiits(arr));
    }

    public void radixSort(int[] arr, int l, int r, int b) {

    }

//    public int maxbiits(int[] arr) {
//        return
//    }
    /* 前缀和数组 */
}
